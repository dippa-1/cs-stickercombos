from setuptools import setup, find_packages

setup(
  name='cs-stickercombos',
  version='0.1',
  author='',
  description='',
  packages=find_packages(),
  install_requires=[
    'flask',
    'flask_cors',
    'numpy',
  ],
)
